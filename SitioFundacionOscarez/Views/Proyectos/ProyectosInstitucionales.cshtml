@model SitioFundacionOscarez.Models.Proyectos

@{
    ViewBag.Title = "Programas institucionales";
    var col = 0;
}

<div class="row">
    <div class="col-lg-12">
        <h2 class="page-header">Que hacemos <small> - programas institucionales</small></h2>
        <ol class="breadcrumb">
            <li>@Html.ActionLink("Inicio", "Index", "Home")</li>
            <li class="active">Programas institucionales</li>
        </ol>
    </div>
</div>

@if (@ViewBag.Error == null)
{
    if (@ViewBag.Proyectos != null)
    {
        foreach (var item in @ViewBag.Proyectos)
        {
            if (col % 2 == 0)
            {
                @:<div class="row">
            }
            <div class="col-md-6 img-portfolio">
                <a href="/Details/@item.id">
                    <img class="img-responsive img-hover" src="/Images/proyectos/@item.ImagenProyecto" alt="Proyectos/Details/@item.Titulo">
                </a>
                <h3><a href="/Details/@item.id">@item.Titulo</a></h3>
                <h4>@item.Subtitulo</h4>
                <p style=" text-align: justify;">
                    @MvcHtmlString.Create(@item.Descripcion.Substring(0, 120)) ...
                </p>
            </div>
            if (col % 2 != 0)
            {
               @:</div>
            }
            col++;
        }
    }
}
else
{
    <div class="alert alert-danger alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <strong>Sr Usuario! </strong>
        <br />
        @ViewBag.Error
    </div>
}